# Profile:Dev
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/housing?characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: chn971229
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.zaxxer.hikari.HikariDataSource

  redis:
    host: localhost
    timeout: 5000ms
    port: 6379
    lettuce:
      pool:
        max-wait: 5000ms

logging:
  config: classpath:log/log4j-dev.xml

